/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { pricesGetPriceByLookupKey } from "../../funcs/pricesGetPriceByLookupKey.js";
import { GetPriceByLookupKeyRequest$zodSchema } from "../../models/getpricebylookupkeyop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetPriceByLookupKeyRequest$zodSchema,
};

export const tool$pricesGetPriceByLookupKey: ToolDefinition<typeof args> = {
  name: "get-price-by-lookup-key",
  description: `Get price by lookup key

Use when resolving a price by external id (e.g. from your catalog or CMS). Ideal for integrations.`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await pricesGetPriceByLookupKey(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
