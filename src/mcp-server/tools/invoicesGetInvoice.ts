/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { invoicesGetInvoice } from "../../funcs/invoicesGetInvoice.js";
import { GetInvoiceRequest$zodSchema } from "../../models/getinvoiceop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetInvoiceRequest$zodSchema,
};

export const tool$invoicesGetInvoice: ToolDefinition<typeof args> = {
  name: "get-invoice",
  description: `Get invoice

Use when loading an invoice for display or editing (e.g. portal or reconciliation). Supports group_by for usage breakdown and force_runtime_recalculation.`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await invoicesGetInvoice(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
