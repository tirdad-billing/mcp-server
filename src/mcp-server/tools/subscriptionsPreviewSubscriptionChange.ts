/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { subscriptionsPreviewSubscriptionChange } from "../../funcs/subscriptionsPreviewSubscriptionChange.js";
import { PreviewSubscriptionChangeRequest$zodSchema } from "../../models/previewsubscriptionchangeop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: PreviewSubscriptionChangeRequest$zodSchema,
};

export const tool$subscriptionsPreviewSubscriptionChange: ToolDefinition<
  typeof args
> = {
  name: "preview-subscription-change",
  description: `Preview subscription plan change

Use when showing a customer the cost of a plan change before they confirm (e.g. upgrade/downgrade preview with proration).`,
  scopes: ["write"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await subscriptionsPreviewSubscriptionChange(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
