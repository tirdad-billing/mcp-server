/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { invoicesGetCustomerInvoiceSummary } from "../../funcs/invoicesGetCustomerInvoiceSummary.js";
import { GetCustomerInvoiceSummaryRequest$zodSchema } from "../../models/getcustomerinvoicesummaryop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetCustomerInvoiceSummaryRequest$zodSchema,
};

export const tool$invoicesGetCustomerInvoiceSummary: ToolDefinition<
  typeof args
> = {
  name: "get-customer-invoice-summary",
  description: `Get customer invoice summary

Use when showing a customer's invoice overview (e.g. billing portal or balance summary). Includes totals and multi-currency breakdown.`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await invoicesGetCustomerInvoiceSummary(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
