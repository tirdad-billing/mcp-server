/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { plansSyncPlanPrices } from "../../funcs/plansSyncPlanPrices.js";
import { SyncPlanPricesRequest$zodSchema } from "../../models/syncplanpricesop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: SyncPlanPricesRequest$zodSchema,
};

export const tool$plansSyncPlanPrices: ToolDefinition<typeof args> = {
  name: "sync-plan-prices",
  description: `Synchronize plan prices

Use when you have changed plan prices and need to push them to all active subscriptions (e.g. global price update). Returns workflow ID.`,
  scopes: ["write"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await plansSyncPlanPrices(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
