/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { pricesGetPrice } from "../../funcs/pricesGetPrice.js";
import { GetPriceRequest$zodSchema } from "../../models/getpriceop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetPriceRequest$zodSchema,
};

export const tool$pricesGetPrice: ToolDefinition<typeof args> = {
  name: "get-price",
  description: `Get price

Use when you need to load a single price (e.g. for display or editing). Response includes expanded meter and price unit when applicable.`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await pricesGetPrice(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
