/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { invoicesFinalizeInvoice } from "../../funcs/invoicesFinalizeInvoice.js";
import { FinalizeInvoiceRequest$zodSchema } from "../../models/finalizeinvoiceop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: FinalizeInvoiceRequest$zodSchema,
};

export const tool$invoicesFinalizeInvoice: ToolDefinition<typeof args> = {
  name: "finalize-invoice",
  description: `Finalize invoice

Use when locking an invoice for payment (e.g. after review). Once finalized, line items are locked; invoice can be paid or voided.`,
  scopes: ["delete"],
  annotations: {
    "title": "",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await invoicesFinalizeInvoice(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
