/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { subscriptionsGetSubscriptionUpcomingGrants } from "../../funcs/subscriptionsGetSubscriptionUpcomingGrants.js";
import { GetSubscriptionUpcomingGrantsRequest$zodSchema } from "../../models/getsubscriptionupcominggrantsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetSubscriptionUpcomingGrantsRequest$zodSchema,
};

export const tool$subscriptionsGetSubscriptionUpcomingGrants: ToolDefinition<
  typeof args
> = {
  name: "get-subscription-upcoming-grants",
  description: `Get upcoming credit grant applications

Use when showing upcoming or pending credits for a subscription (e.g. in a portal or for forecasting).`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await subscriptionsGetSubscriptionUpcomingGrants(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
