/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { subscriptionsExecuteSubscriptionChange } from "../../funcs/subscriptionsExecuteSubscriptionChange.js";
import { ExecuteSubscriptionChangeRequest$zodSchema } from "../../models/executesubscriptionchangeop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ExecuteSubscriptionChangeRequest$zodSchema,
};

export const tool$subscriptionsExecuteSubscriptionChange: ToolDefinition<
  typeof args
> = {
  name: "execute-subscription-change",
  description: `Execute subscription plan change

Use when applying a plan change (e.g. upgrade or downgrade). Executes proration and generates invoice or credit as needed.`,
  scopes: ["write"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await subscriptionsExecuteSubscriptionChange(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
